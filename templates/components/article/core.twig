{##
 # Template:    Article
 # Description: Core article component.
 # Package:     Xavier
##}

{% set format = fn( 'get_post_format' ) %}

<article id="post-{{ post.ID }}" {{ post.post_class }}>
	{% include [
		'components/article/twig/format/' ~ format ~ '.twig',
		'components/article/twig/default.twig',
	] %}
</article>
